class AppModel {
  DateTime toDoListDate = DateTime(DateTime.now().year, DateTime.now().month,
      DateTime.now().day, 0, 0, 0, 0, 0);
  int screenIndex = 0; //индекс вкладки на основной странице
  DateTime dateTimeNow = DateTime.now();
}
